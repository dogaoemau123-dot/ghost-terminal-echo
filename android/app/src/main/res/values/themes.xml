<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.HACKER" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="colorPrimary">@color/neon_green</item>
        <item name="colorSecondary">@color/neon_purple</item>
    </style>

    <style name="Theme.HACKER.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.HACKER</item>
    </style>
</resources>
